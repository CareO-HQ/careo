diff --git a/lib/permissions.ts b/lib/permissions.ts
index c3b806a..35f865e 100644
--- a/lib/permissions.ts
+++ b/lib/permissions.ts
@@ -66,6 +66,10 @@ export function canViewSidebarStaff(role?: string): boolean {
   return role === "owner" || role === "manager" || role === "saas_admin";
 }
 
+export function canViewSidebarAgencyStaff(role?: string): boolean {
+  return role === "owner" || role === "manager" || role === "saas_admin";
+}
+
 export function canViewSidebarHandover(role?: string): boolean {
   return role === "owner" || role === "manager" || role === "nurse" || role === "saas_admin" || role === "care_assistant";
 }
@@ -141,7 +145,7 @@ export function canAddDietMenu(role?: string): boolean {
 }
 
 export function canManageMenu(role?: string): boolean {
-  return role === "manager" || role === "nurse";
+  return role === "nurse";
 }
 
 export function canLogFoodFluidEntry(role?: string): boolean {
@@ -298,6 +302,9 @@ export function canViewResidentSection(section: string, role?: string): boolean
       return canLogDailyCare(role);
     case "progress-notes":
       return canViewProgressNotes(role);
+    case "continence":
+      // All users can view; care assistant included explicitly
+      return canLogDailyCare(role);
     case "documents":
       return canViewDocuments(role);
     case "night-check":
