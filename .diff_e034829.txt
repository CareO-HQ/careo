commit e034829ec6df26a822132ae2d86f04208191911f
Author: Abi George <contactabisgeorge@gmail.com>
Date:   Mon Mar 23 01:11:39 2026 +0000

    Update progress notes and add end of life folder
    
    - Remove submit button from top bar
    - Reduce care file numbers to 18 with tooltips
    - Add End of Life / Death folder
    
    ≡ƒñû Generated with [Claude Code](https://claude.com/claude-code)
    
    Co-Authored-By: Claude <noreply@anthropic.com>

diff --git a/app/(dashboard)/dashboard/residents/[id]/(pages)/care-file-v2/[folderKey]/page.tsx b/app/(dashboard)/dashboard/residents/[id]/(pages)/care-file-v2/[folderKey]/page.tsx
index 4087f08..96a7c2e 100644
--- a/app/(dashboard)/dashboard/residents/[id]/(pages)/care-file-v2/[folderKey]/page.tsx
+++ b/app/(dashboard)/dashboard/residents/[id]/(pages)/care-file-v2/[folderKey]/page.tsx
@@ -596,9 +596,6 @@ export default function CareFileV2FolderPage() {
                                         ) : (
                                             <>
                                                 <Button variant="outline" size="sm" onClick={handleCloseForm} disabled={isSaving}>Cancel</Button>
-                                                <Button size="sm" onClick={handleExternalSubmit} disabled={isSaving} className="gap-2">
-                                                    {isSaving ? <Loader2 className="w-4 h-4 animate-spin" /> : <FileText className="w-4 h-4" />} Submit
-                                                </Button>
                                             </>
                                         )}
                                         <Button
diff --git a/components/residents/carefile/folders/ProgressNoteForm.tsx b/components/residents/carefile/folders/ProgressNoteForm.tsx
index 24e8a4d..52d5469 100644
--- a/components/residents/carefile/folders/ProgressNoteForm.tsx
+++ b/components/residents/carefile/folders/ProgressNoteForm.tsx
@@ -14,6 +14,28 @@ import { Resident } from "@/types";
 
 const UK_TIMEZONE = "Europe/London";
 
+// Map care file numbers to folder names
+const CARE_FILE_NAMES: Record<number, string> = {
+    1: "Pre-Admission",
+    2: "Admission",
+    3: "Maintaining a Safe Environment",
+    4: "Dependency",
+    5: "This Is My Life",
+    6: "Medication",
+    7: "Mobility",
+    8: "Nutrition and Hydration",
+    9: "Incontinence",
+    10: "Personal Hygiene and Dressing",
+    11: "Skin Integrity / Tissue Viability",
+    12: "Additional Care Plans",
+    13: "Psychological & Emotional Needs",
+    14: "Residents' Valuables and Personal Property",
+    15: "Record of Specimens",
+    16: "Confidential Records",
+    17: "Safeguarding & DoLS",
+    18: "Key Worker Diary",
+};
+
 interface ProgressNoteFormProps {
     residentId: string;
     resident: Resident;
@@ -47,6 +69,7 @@ export function ProgressNoteForm({ residentId, resident }: ProgressNoteFormProps
     const [selectedCareFileNumbers, setSelectedCareFileNumbers] = useState<number[]>([]);
     const [editingNoteId, setEditingNoteId] = useState<string | null>(null);
     const [isEditing, setIsEditing] = useState(false);
+    const [hoveredCareFile, setHoveredCareFile] = useState<number | null>(null);
 
     const fetchProgressNotes = async () => {
         if (!residentId) return;
@@ -323,20 +346,29 @@ export function ProgressNoteForm({ residentId, resident }: ProgressNoteFormProps
 
                         <div className="space-y-1.5">
                             <label className="text-xs font-medium text-muted-foreground">Care File Numbers (Select multiple)</label>
-                            <div className="grid grid-cols-5 sm:grid-cols-10 gap-2 p-3 border rounded-md bg-background">
-                                {Array.from({ length: 20 }, (_, i) => i + 1).map((num) => (
-                                    <button
-                                        key={num}
-                                        type="button"
-                                        onClick={() => toggleCareFileNumber(num)}
-                                        className={`h-8 w-8 rounded-md border text-xs font-medium transition-all ${
-                                            selectedCareFileNumbers.includes(num)
-                                                ? "bg-primary text-primary-foreground border-primary"
-                                                : "bg-background hover:bg-muted border-input"
-                                        }`}
-                                    >
-                                        {num}
-                                    </button>
+                            <div className="grid grid-cols-6 sm:grid-cols-9 gap-2 p-3 border rounded-md bg-background relative">
+                                {Array.from({ length: 18 }, (_, i) => i + 1).map((num) => (
+                                    <div key={num} className="relative">
+                                        <button
+                                            type="button"
+                                            onClick={() => toggleCareFileNumber(num)}
+                                            onMouseEnter={() => setHoveredCareFile(num)}
+                                            onMouseLeave={() => setHoveredCareFile(null)}
+                                            className={`h-8 w-8 rounded-md border text-xs font-medium transition-all ${
+                                                selectedCareFileNumbers.includes(num)
+                                                    ? "bg-primary text-primary-foreground border-primary"
+                                                    : "bg-background hover:bg-muted border-input"
+                                            }`}
+                                        >
+                                            {num}
+                                        </button>
+                                        {hoveredCareFile === num && (
+                                            <div className="absolute bottom-full left-1/2 -translate-x-1/2 mb-2 px-3 py-2 bg-blue-50 border border-blue-200 rounded-md shadow-lg text-xs font-medium text-blue-900 whitespace-nowrap z-50 pointer-events-none">
+                                                {CARE_FILE_NAMES[num]}
+                                                <div className="absolute top-full left-1/2 -translate-x-1/2 -mt-1 w-2 h-2 bg-blue-50 border-r border-b border-blue-200 transform rotate-45"></div>
+                                            </div>
+                                        )}
+                                    </div>
                                 ))}
                             </div>
                             {selectedCareFileNumbers.length > 0 && (
diff --git a/config.ts b/config.ts
index fe828eb..8e40b4d 100644
--- a/config.ts
+++ b/config.ts
@@ -543,6 +543,14 @@ export const config = {
       forms: [
         { type: "form", key: "progress-note-form", value: "Progress Notes" }
       ]
+    },
+    {
+      type: "folder",
+      key: "v2-end-of-life",
+      value: "End of Life / Death",
+      description: "End of life care planning and death certification.",
+      carePlan: true,
+      forms: []
     }
   ]
 };
diff --git a/supabase/migrations/20260323000000_add_care_file_numbers_to_progress_notes.sql b/supabase/migrations/20260323000000_add_care_file_numbers_to_progress_notes.sql
index 2c775be..72a7468 100644
--- a/supabase/migrations/20260323000000_add_care_file_numbers_to_progress_notes.sql
+++ b/supabase/migrations/20260323000000_add_care_file_numbers_to_progress_notes.sql
@@ -1,5 +1,5 @@
 -- Add care_file_numbers column to progress_notes table
--- This allows tracking which care file numbers are associated with each progress note
+-- This allows tracking which care file numbers (1-18) are associated with each progress note
 
 ALTER TABLE public.progress_notes
   ADD COLUMN IF NOT EXISTS care_file_numbers INTEGER[] DEFAULT '{}';
@@ -8,4 +8,4 @@ ALTER TABLE public.progress_notes
 CREATE INDEX IF NOT EXISTS idx_progress_notes_care_file_numbers ON public.progress_notes USING GIN(care_file_numbers);
 
 -- Add comment to explain the column
-COMMENT ON COLUMN public.progress_notes.care_file_numbers IS 'Array of care file numbers (1-20) associated with this progress note';
+COMMENT ON COLUMN public.progress_notes.care_file_numbers IS 'Array of care file numbers (1-18) associated with this progress note';
