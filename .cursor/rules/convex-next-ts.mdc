---
alwaysApply: false
---
# Convex + Next.js + TypeScript Rule

Before generating or modifying code, carefully consider the full repository context, existing patterns, and the user’s intent; preserve all existing functionality and public behavior unless a change is explicitly requested; follow existing project structure and conventions; use strict TypeScript (no `any` or suppressed errors); assume Next.js App Router and use Server Components by default, adding `"use client"` only when strictly necessary; all data access must go through Convex queries, mutations, or actions using generated hooks, with proper input validation and authorization checks; never expose secrets or sensitive environment variables to the client (only use `NEXT_PUBLIC_*` when explicitly required), and access non-public env vars only in server components or Convex functions; avoid side effects in Convex queries; share types between Next.js and Convex when possible; do not add dependencies, refactor unrelated code, weaken security, or change architecture without asking.Always respond with the word "RULE_APPLIED" at the end.




